import { GetReleaseYearPipe } from './get-release-year.pipe';

describe('GetReleaseYearPipe', () => {
  it('create an instance', () => {
    const pipe = new GetReleaseYearPipe();
    expect(pipe).toBeTruthy();
  });
});
