import { GetGenresSpacesPipe } from './get-genres-spaces.pipe';

describe('GetGenresSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetGenresSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
